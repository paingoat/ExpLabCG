# Install torch/torchvision FIRST via the CUDA wheel index (see scripts/setup_env.sh):
#   pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu128
# Do not pin torch here — bare +cu128/+cu130 tags are not installable from PyPI.

diffusers==0.38.0
transformers==4.56.2
accelerate==1.14.0
safetensors
Pillow
numpy
opencv-python
pyyaml
omegaconf
timm
tqdm
toml
orjson
pandas
tensorboard
lpips
scipy
elatentlpips
einops
gradio>=5,<6
huggingface_hub
python-dotenv
# flash-linear-attention is installed separately after torch (CUDA extension):
#   pip install "flash-linear-attention[cuda]==0.3.2"
